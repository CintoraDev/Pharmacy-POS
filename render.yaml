services:
  - type: web
    name: Pharmacy-POS
    env: python
    buildCommand: |
      apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0 libffi-dev libcairo2 libpangocairo-1.0-0
      pip install -r requirements.txt
    startCommand: gunicorn app:app